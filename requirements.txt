# Core Discord Bot Requirements
discord.py[voice]==2.3.2
PyNaCl==1.5.0

# Audio Processing
pydub==0.25.1
numpy==1.24.3

# Text-to-Speech Options (install at least one)
# Option 1: Google TTS (simple, requires internet)
gTTS==2.4.0

# Option 2: Offline TTS (basic quality)
pyttsx3==2.90

# Option 3: Advanced Voice Cloning (recommended, best quality)
# Uncomment to use Coqui TTS for voice cloning:
# TTS==0.22.0
# torch>=2.0.0
# torchaudio>=2.0.0

# Environment Variables
python-dotenv==1.0.0

# Utilities
requests==2.31.0
aiohttp==3.9.1

# Note: FFmpeg is required for audio processing
# Install separately:
# Ubuntu/Debian: sudo apt-get install ffmpeg
# macOS: brew install ffmpeg
# Windows: Download from https://ffmpeg.org/download.html
